<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.microservice.workorder.service.repository.WorkorderRepository">

    <select id="selectAll" resultType="Map">
        select

         ifnull( WORKORDER_ID,'' )		WORKORDER_ID,
        ifnull( SERIAL_NUM,'' )		SERIAL_NUM,
        ifnull( CONNECTTIME,'' )		CONNECTTIME,
        ifnull( BLOCKING_TIME,'' )		BLOCKING_TIME,
        ifnull( AREA,'' )		AREA,
        ifnull( OFFICE_DIRECTION,'' )		OFFICE_DIRECTION,
        ifnull( SLA,'' )		SLA,
        ifnull( SLA_TAG,'' )		SLA_TAG,
        ifnull( POST,'' )		POST,
        ifnull( SEND_EQUIPMENT,'' )		SEND_EQUIPMENT,
        ifnull( APPLICATION_DATE,'' )		APPLICATION_DATE,
        ifnull( ARRIVE_TIME,'' )		ARRIVE_TIME,
        ifnull( COMPLETION_TIME,'' )		COMPLETION_TIME,
        ifnull( CONSTRUCTION_ACTION,'' )		CONSTRUCTION_ACTION,
        ifnull( CONSTRUCTION_TYPE,'' )		CONSTRUCTION_TYPE,
        ifnull( PRODUCT_TYPE,'' )		PRODUCT_TYPE,
        ifnull( ORDER_STATE,'' )		ORDER_STATE,
        ifnull( MARK,'' )		MARK,
        ifnull( INFORMATIONAD_ID,'' )		INFORMATIONAD_ID,
        ifnull( INFORMATION_TELEPHONE_ID,'' )		INFORMATION_TELEPHONE_ID,
        ifnull( INFORMATION_LAN_ID,'' )		INFORMATION_LAN_ID,
        ifnull( ELECTRONIC_ID,'' )		ELECTRONIC_ID,
        ifnull( USER_ID,'' )		USER_ID,
        ifnull( EPON_ID,'' )		EPON_ID,
        ifnull( FTTH_ID,'' )		FTTH_ID,
        ifnull( ORDER_RESPONSIBLE,'' )		ORDER_RESPONSIBLE,
        ifnull( ORGANIZATION_ID,'' )		ORGANIZATION_ID,
        ifnull( TIME_LIMIT,'' )		TIME_LIMIT,
        ifnull( SEND_ORDER_TIME,'' )		SEND_ORDER_TIME,
        ifnull( WORK_APPOINTMENT_END_TIME,'' )		WORK_APPOINTMENT_END_TIME,
        ifnull( WORK_APPOINTMENT_START_TIME,'' )		WORK_APPOINTMENT_START_TIME,
        ifnull( ORDER_REQ_START_TIME,'' )		ORDER_REQ_START_TIME,
        ifnull( ORDER_REQ_END_TIME,'' )		ORDER_REQ_END_TIME,
        ifnull( TAKE_TIME,'' )		TAKE_TIME,
        ifnull( ORDER_WHETHER_OVERTIME,'' )		ORDER_WHETHER_OVERTIME,
        ifnull( X_POSITION,'' )		X_POSITION,
        ifnull( Y_POSITION,'' )		Y_POSITION,
        ifnull( PORT_,'' )		PORT_,
        ifnull( LINE,'' )		LINE,
        ifnull( PAIR,'' )		PAIR,
        ifnull( USER_NAME,'' )		USER_NAME,
        ifnull( USER_NUM,'' )		USER_NUM,
        ifnull( ADDRESS,'' )		ADDRESS,
        ifnull( PHONE,'' )		PHONE,
        ifnull( ASSOCIATION_NUM,'' )		ASSOCIATION_NUM,
        ifnull( BANDWIDTH_ACCOUNT,'' )		BANDWIDTH_ACCOUNT,
        ifnull( ACCOUNT_PASSWORD,'' )		ACCOUNT_PASSWORD,
        ifnull( APPLICATION_NO,'' )		APPLICATION_NO,
        ifnull( CLIENT_BELONG,'' )		CLIENT_BELONG,
        ifnull( DEVICE_NUMBER,'' )		DEVICE_NUMBER,
        ifnull( CONNECTIONPEOPLE,'' )		CONNECTIONPEOPLE,
        ifnull( RETURNPEOPLE,'' )		RETURNPEOPLE,
        ifnull( RETURNTIME,'' )		RETURNTIME,
        ifnull( MODFITIME,'' )		MODFITIME,
        ifnull( JOB_TYPE,'' )		JOB_TYPE,
        ifnull( STATUS_,'' )		STATUS_,
        ifnull( EXTRACT_DATE,'' )		EXTRACT_DATE,
        ifnull( RSOW_INIPMS,'' )		RSOW_INIPMS,
        ifnull( AGE_CAT,'' )		AGE_CAT,
        ifnull( AGE,'' )		AGE,
        ifnull( SO_TYPE,'' )		SO_TYPE,
        ifnull( EXCHANGE_,'' )		EXCHANGE_,
        ifnull( CABINET,'' )		CABINET,
        ifnull( PROJECT_ID,'' )		PROJECT_ID,
        ifnull( CONTRACTOR,'' )		CONTRACTOR,
        ifnull( OFC,'' )		OFC,
        ifnull( PERAREA,'' )		PERAREA,
        ifnull( SO_NUMBER,'' )		SO_NUMBER,
        ifnull( INSTALLER,'' )		INSTALLER,
        ifnull( SERVICE_TYPE,'' )		SERVICE_TYPE,
        ifnull( APPROVER_NAME,'' )		APPROVER_NAME,
        ifnull( CUSTOMER_NAME,'' )		CUSTOMER_NAME,
        ifnull( CUSTOMER_ADDRESS,'' )		CUSTOMER_ADDRESS,
        ifnull( MKT_SGMT,'' )		MKT_SGMT,
        ifnull( TECH_ID,'' )		TECH_ID,
        ifnull( ASSIGN_TEL_NO,'' )		ASSIGN_TEL_NO,
        ifnull( PLAN,'' )		PLAN,
        ifnull( NE_TYPE,'' )		NE_TYPE,
        ifnull( PRIORITY,'' )		PRIORITY,
        ifnull( PING,'' )		PING,
        ifnull( UPLOAD_SPEED,'' )		UPLOAD_SPEED,
        ifnull( DOWNLOAD_SPEED,'' )		DOWNLOAD_SPEED,
        ifnull( CBR,'' )		CBR,
        ifnull( SEQ,'' )		SEQ,
        ifnull( XCONNECT,'' )		XCONNECT,
        ifnull( LOCATION1,'' )		LOCATION1,
        ifnull( TYPE1,'' )		TYPE1,
        ifnull( INDEX1,'' )		INDEX1,
        ifnull( PORT1,'' )		PORT1,
        ifnull( LOCATION2,'' )		LOCATION2,
        ifnull( TYPE2,'' )		TYPE2,
        ifnull( INDEX2,'' )		INDEX2,
        ifnull( PORT2,'' )		PORT2,
        ifnull( LANDMARK,'' )		LANDMARK,
        ifnull( SERVICEID,'' )		SERVICEID,
        ifnull( FILENAME,'' )		FILENAME,
        ifnull( PRIMARY_CABLE_PAIR,'' )		PRIMARY_CABLE_PAIR,
        ifnull( SECONDARY_CABLE_PAIR,'' )		SECONDARY_CABLE_PAIR,
        ifnull( LCP,'' )		LCP,
        ifnull( FTTH_NAP_PAIR,'' )		FTTH_NAP_PAIR,
        ifnull( NODE_NAME,'' )		NODE_NAME,
        ifnull( I_OK,'' )		I_OK,
        ifnull( ACTIVATED,'' )		ACTIVATED,
        ifnull( RESCUE_PERSON,'' )		RESCUE_PERSON,
        ifnull( CONSUME_STATUS,'' )		CONSUME_STATUS,
        ifnull( SLA_TOTAL,'' )		SLA_TOTAL,
        ifnull( SPAN,'' )		SPAN,
        ifnull( CONSUME_TYPE,'' )		CONSUME_TYPE,
        ifnull( PACKAGE_CONSUME,'' )		PACKAGE_CONSUME,
        ifnull( FIRST_PASS,'' )		FIRST_PASS,
        ifnull( SECOND_PASS,'' )		SECOND_PASS,
        ifnull( BILLING_RETURN_REASON,'' )		BILLING_RETURN_REASON,
        ifnull( BILLING_STATUS,'' )		BILLING_STATUS,
        ifnull( HOF_STATUS,'' )		HOF_STATUS,
        ifnull( HOF_UPLOAD_DATE,'' )		HOF_UPLOAD_DATE,
        ifnull( HOF_UPLOADED_BY,'' )		HOF_UPLOADED_BY,
        ifnull( ACTUAL_FACILITY,'' )		ACTUAL_FACILITY,
        ifnull( PRIMARY_,'' )		PRIMARY_,
        ifnull( CAB_SEC,'' )		CAB_SEC,
        ifnull( TRUCK_ROLL,'' )		TRUCK_ROLL,
        ifnull( PROJECT_TYPE,'' )		PROJECT_TYPE,
        ifnull( THIRD_PASS,'' )		THIRD_PASS,
        ifnull( FOURTH_PASS,'' )		FOURTH_PASS,
        ifnull( SPECIAL_PASS,'' )		SPECIAL_PASS,
        ifnull( TICKET_MAKE_UP,'' )		TICKET_MAKE_UP,
        ifnull( MAKE_UP_DATE,'' )		MAKE_UP_DATE,
        ifnull( ATTENDED_DATE,'' )		ATTENDED_DATE,
        ifnull( FACILITY_,'' )		FACILITY_,
        ifnull( METER_MARK_START,'' )		METER_MARK_START,
        ifnull( METER_MARK_END,'' )		METER_MARK_END,
        ifnull( IOO_READING,'' )		IOO_READING,
        ifnull( METER_MARK_START_PHOTO,'' )		METER_MARK_START_PHOTO,
        ifnull( METER_MARK_END_PHOTO,'' )		METER_MARK_END_PHOTO,
        ifnull( SUBSCRIBER_SIGNATURE,'' )		SUBSCRIBER_SIGNATURE,
        ifnull( PR_DATA,'' )		PR_DATA,
        ifnull( EASY_DISPATCH,'' )		EASY_DISPATCH,
        ifnull( EASY_DISPATCH_DATE,'' )		EASY_DISPATCH_DATE

         from work_order_
    </select>

    <select id="selectTableColumns" resultType="Map">
        SELECT COLUMN_NAME
          FROM INFORMATION_SCHEMA.COLUMNS
          WHERE  TABLE_NAME = 'work_order_'
    </select>

</mapper>